import { Group } from "three";

export class WaterSimulation extends Group {

  constructor() {
    super();
  }

  async init() {
  }

  update(delta: number) {
    // TODO: will remove
    delta = 0;
  }

  render() {}
}